name: dmart
description: A pure Dart implementation of the Dmart client that depends on dio.
version: 2.9.0
repository: https://github.com/edraj/flutterdmart

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

environment:
  sdk: ^3.8.0

# Add regular dependencies here.
dependencies:
  dio: ^5.11.0
  http_parser: ^4.1.2

dev_dependencies:
  lints: ^6.1.0
  test: ^1.31.2
